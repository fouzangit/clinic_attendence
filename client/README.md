# Client (Frontend)

This folder contains the React frontend for the Clinic Management System.

## Getting Started

- Run `npm install` to install dependencies.
- Run `npm start` to start the development server.

Add your React app source code in the `src/` directory.